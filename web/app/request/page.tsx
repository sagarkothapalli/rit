"use client";

import RequestWizard from "@/components/request/RequestWizard";

/** Backward-compatible alias for the new-request wizard. */
export default function RequestPage() {
  return <RequestWizard />;
}
